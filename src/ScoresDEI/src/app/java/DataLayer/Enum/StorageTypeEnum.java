package DataLayer.Enum;

/**
 * Enum that tells where the data is stored, if locally the type is FILE_SYSTEM and if remotely the type is HTTP.
 */
public enum StorageTypeEnum {
    FILE_SYSTEM, HTTP
}
